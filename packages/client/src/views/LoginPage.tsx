export function LoginPage() {
  return (
    <section className="panel">
      <h2>Authentication scaffold</h2>
      <p>
        Login, refresh-token recovery, and protected routes arrive in the next
        commit. No fake auth shortcuts. Johnny would notice.
      </p>
    </section>
  );
}
