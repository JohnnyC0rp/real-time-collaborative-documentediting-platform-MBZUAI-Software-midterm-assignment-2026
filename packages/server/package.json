{
  "name": "@collab/server",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "check": "python3 -m compileall app"
  }
}
